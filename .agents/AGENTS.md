# Deployment Rule
Whenever any code changes, fixes, or feature updates are completed for this repository, ALWAYS deploy the changes to GitHub (via `git commit` and `git push origin main`) and Vercel. Since pushing to GitHub's main branch triggers a Vercel deployment automatically on this repository, pushing to GitHub is sufficient, but ensure both are mentioned and completed.
